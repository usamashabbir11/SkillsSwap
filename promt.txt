In frontend/src/components/EditProfile.jsx, change the location detection logic.

Current behavior: detectLocation overwrites the city field completely.

New behavior:
- User types their city name manually in the city field (e.g. "Lahore")
- When user clicks "Detect My Location", it detects the precise area/suburb only
- Then combine: area + ", " + existing city value
- Example: user typed "Lahore", detection finds "Gulberg" → city field becomes "Gulberg, Lahore"
- If city field is empty and detection finds area → just use the area
- If detection finds nothing → keep existing city field unchanged
- Success message: "✅ Location detected: Gulberg, Lahore"

Only change the detectLocation function. Do not touch anything else in the file. Follow all rules in CLAUDE.md strictly.