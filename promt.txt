Add delete course functionality.

BACKEND:
1. In backend/controllers/userController.js, add a new function deleteCourse:
   - Route will be DELETE /users/courses/:courseIndex
   - Get the course index from req.params.courseIndex
   - Find the logged in user
   - Remove the course at that index from user.courses array using splice
   - Save the user
   - Return success

2. In backend/routes/userRoutes.js, add:
   - DELETE /courses/:courseIndex protected with getLoggedInUser middleware

FRONTEND:
3. In frontend/src/api/userApi.js, add at the bottom:
   - deleteCourseApi(courseIndex) — calls DELETE /users/courses/:courseIndex

4. In frontend/src/components/Profile.jsx, add a delete button on each course card:
   - Small red trash/delete button at top right corner of each course card
   - Show a confirm dialog: "Are you sure you want to delete this course? This cannot be undone."
   - On confirm call deleteCourseApi(index)
   - On success remove the course from the local state immediately
   - Do not show delete button on UserProfile.jsx (only own profile)

Read all relevant files first. Follow all rules in CLAUDE.md strictly.