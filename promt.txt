Build a complete Complaints System AND redesign the Admin Dashboard. This involves both backend and frontend changes.

STRICT SAFETY RULES:
- Do NOT modify any existing endpoints
- Do NOT modify any existing models
- Do NOT break any existing features
- Only ADD new files and new endpoints
- Read every file before touching it

BACKEND CHANGES:

1. Create backend/models/complaintModel.js:
   - reporter: ObjectId ref User (required)
   - reportedUser: ObjectId ref User (required)
   - reason: String enum ["Fake Profile", "Inappropriate Content", "Spam", "Harassment", "Other"] required
   - description: String default ""
   - status: String enum ["pending", "resolved", "dismissed"] default "pending"
   - timestamps: true

2. Create backend/controllers/complaintController.js with these functions:
   - submitComplaint(req, res): logged in user submits complaint against another user. Check user cannot report themselves. Check no duplicate pending complaint against same user. Create complaint. Return success.
   - getComplaints(req, res): admin only. Returns all complaints with reporter and reportedUser populated (name, email, profileImage). Sort by createdAt desc.
   - updateComplaintStatus(req, res): admin only. Updates status to resolved or dismissed.

3. Create backend/routes/complaintRoutes.js:
   - POST /complaints — protected with getLoggedInUser
   - GET /complaints — protected with getLoggedInUser + requireAdmin
   - PUT /complaints/:id — protected with getLoggedInUser + requireAdmin

4. Register in backend/app.js:
   - Add: import complaintRoutes from "./routes/complaintRoutes.js"
   - Add: app.use("/complaints", complaintRoutes)
   - Do not change anything else in app.js

5. Update backend/routes/adminRoutes.js or adminController.js:
   - The existing GET /admin/stats endpoint should also return totalComplaints count
   - Read the file first and add only the complaints count to the existing stats response

FRONTEND CHANGES:

6. Add to frontend/src/api/userApi.js (at the bottom under /* COMPLAINTS */):
   - submitComplaintApi(reportedUserId, reason, description)
   - getComplaintsApi()
   - updateComplaintStatusApi(complaintId, status)

7. Update frontend/src/components/UserProfile.jsx:
   - Add a "Report User" button in the profile header area (small, subtle, gray color, not prominent)
   - When clicked opens a small modal/form with:
     - Dropdown: Select reason (Fake Profile, Inappropriate Content, Spam, Harassment, Other)
     - Textarea: Description (optional, placeholder "Describe the issue...")
     - Submit button and Cancel button
   - On submit calls submitComplaintApi
   - Show success message "Report submitted successfully"
   - Do not change any other existing feature in UserProfile.jsx

8. Completely redesign frontend/src/pages/AdminDashboard.jsx with premium dark sidebar layout:

   LAYOUT: Two column — dark navy sidebar (240px) + white main area

   DARK SIDEBAR:
   - Background: #1a1a2e
   - Logo: SkillsSwap with ADMIN badge in green
   - Nav sections: Overview (Dashboard, Analytics), Management (All Users, Complaints, Courses, Payments), Settings
   - Active item: green left border + green text + subtle green background
   - Bottom: admin name and role
   - Nav items navigate to correct routes

   TOP BAR:
   - White, height 56px, "Admin Dashboard" title
   - Right: "Welcome back, Usama" + green avatar

   STATS CARDS (4 in a row):
   - Total Users (green accent)
   - Total Swaps (blue accent)
   - Total Courses (purple accent)
   - Total Revenue from purchases (orange accent)
   - Each card shows count + colored top accent bar + trend text

   CHARTS ROW (2 columns):
   - Weekly Signups bar chart (use simple CSS bars, no library needed)
   - Recent Complaints table showing reporter name, reported user, reason, status badge

   RECENT USERS TABLE (full width):
   - Columns: User, Email, Courses, City, Joined, Actions
   - Actions: View button (navigates to /users/:id) + Delete button (calls existing delete API)
   - Fetch real users from GET /users/all

   COMPLAINTS SECTION (full width, below users table):
   - Title: "All Complaints"
   - Table columns: Reporter, Reported User, Reason, Description, Status, Date, Actions
   - Status badges: pending (yellow), resolved (green), dismissed (gray)
   - Actions: "Resolve" button + "Dismiss" button (calls updateComplaintStatusApi)
   - Fetch real complaints from GET /complaints

   All data fetched from real APIs. No dummy data except the bar chart.

Read ALL relevant files first before making any changes. Follow all rules in CLAUDE.md strictly.