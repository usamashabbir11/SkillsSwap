Add YouTube-style thumbnail support to courses.

STRICT SAFETY RULES:
- Do NOT break any existing course features
- Do NOT change the course video upload logic
- Existing courses without thumbnail show dark gradient fallback
- Read every file before touching it

BACKEND CHANGES:

1. Update backend/models/userModel.js:
   - In courseSchema add one new optional field:
     thumbnail: { type: String, default: "" }
   - Do not change any other field

2. Update backend/config/cloudinary.js:
   - Add a new CloudinaryStorage for thumbnails:
     folder: "skillsswap/thumbnails"
     allowed_formats: ["jpg", "jpeg", "png", "webp"]
   - Export a new multer instance: uploadThumbnail
   - Do not change uploadProfile, uploadCover, uploadCourse

3. Update backend/routes/userRoutes.js:
   - Change POST /courses route to accept both video and thumbnail:
     use multer fields: [{ name: "video", maxCount: 1 }, { name: "thumbnail", maxCount: 1 }]
   - Import uploadThumbnail from cloudinary config
   - Use a combined upload middleware that handles both fields
   - Do not change any other route

4. Update backend/controllers/userController.js:
   - In addCourse function:
     video = req.files?.video?.[0]?.path || ""
     thumbnail = req.files?.thumbnail?.[0]?.path || ""
   - Save both in the course object
   - Do not change anything else

FRONTEND CHANGES:

5. Update frontend/src/components/EditProfile.jsx:
   - In Add Course section, the video upload box should have a small "+ Add Thumbnail" button INSIDE the upload box at the bottom right corner
   - This button triggers a hidden separate file input for thumbnail (accept="image/*")
   - When thumbnail is selected, show it as background of the upload box with a play button overlay on top
   - Show "Change Thumbnail" button bottom right when thumbnail is selected
   - Show video filename in a green bar at the bottom of the box when video is selected
   - Append thumbnail to FormData: data.append("thumbnail", courseThumbnail) only if selected
   - State: courseThumbnail, courseThumbnailSelected, courseThumbnailPreview (use URL.createObjectURL for preview)
   - Do not change any other field in EditProfile

6. Update frontend/src/components/Profile.jsx:
   - In courses section, for each course:
     If course.thumbnail exists → show as background-image or img covering the course card header area (height 140px, object-fit cover)
     If no thumbnail → show dark gradient with play button icon as before
   - Use getImageUrl helper for the thumbnail URL

7. Update frontend/src/components/UserProfile.jsx:
   - Same as Profile.jsx
   - Show thumbnail if available, dark gradient with play button if not
   - Use existing getImageUrl helper

Read all relevant files first before making any changes. Follow all rules in CLAUDE.md strictly.